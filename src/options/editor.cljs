(ns options.editor
  (:require 
     [options.editor.bool]
     [options.editor.button]
     [options.editor.select]
     [options.editor.string]
     [options.editor.view])
  
   (def bool options.editor.bool/editor-bool)
   (def button options.editor.bool/editor-button)
   (def select options.editor.bool/editor-select)
   (def string options.editor.bool/editor-string)
   (def view options.editor.bool/editor-view)
  
  )